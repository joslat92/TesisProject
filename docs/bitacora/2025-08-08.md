# Bitácora — 2025-08-08

## Objetivo del día
Dejar el repo reproducible, liviano y con hooks en verde; preparar base para cerrar resultados del Cap. 7.

## Cambios clave
- **Pre-commit**: `black`, `ruff`, `mypy` en verde (añadido `types-tabulate`).
- **Typing / fixes**:
  - `tune_lstm*.py`: `param_grid: Dict[str, Sequence[Any]]` y `product(*param_grid.values())`.
  - Asignaciones de columnas: uso de `pd.Index([...])`.
  - `tabulate(DataFrame)`: ahora via `itertuples(index=False)` + `headers=list(cols)`.
- **Seeding centralizado** (`src/seeding.py`):
  - `setup_repro(seed)` fija `random`, `numpy`, `tensorflow`, `PYTHONHASHSEED`.
  - Inyectado antes de TF/Keras + envs:
    ```
    TF_DETERMINISTIC_OPS=1
    TF_ENABLE_ONEDNN_OPTS=0
    TF_CPP_MIN_LOG_LEVEL=2
    ```
  - Arreglo de `\n` literales que rompían parsing.
- **Entorno**: TensorFlow CPU **2.17.1** + Keras **3.11.1**; test mínimo de determinismo OK.
- **Limpieza/LFS**: dejar de trackear `tmp_kmodels/`; `models/` con `.gitkeep`; sin binarios pesados en PR.
- **GitHub/PR**: creado **PR #2** “Repro: centralize RNG seeding + pin TF/Keras + clean LFS” con labels `reproducibility`, `infra`, `cleanup`.

## Estado actual
- Hooks: black ✅ · ruff ✅ · mypy ✅
- Versiones: Python 3.11.9 · TF CPU 2.17.1 · Keras 3.11.1 · NumPy 1.26.4 · Pandas 2.3.0 · Statsmodels 0.14.4
- Datos presentes: `data/df_final_ready.csv`, `data/df_final_ready_plus_vix.csv`
- Modelos: métricas y predicciones disponibles para ARIMA/SARIMA/LSTM_* (HÍBRIDOS 30/40/50 con `model.h5` y preds)

## Decisiones
- Estándar: TF 2.17.1 CPU + Keras 3.x
- `setup_repro(42)` obligatorio en scripts con TF/Keras.
- No reescribir historia de `main`; PR limpio separado.

## Próximos pasos
1) Regenerar predicciones CV y **tabla DM** final.
2) Figuras Cap. 7 (series, boxplot error, walk-forward).
3) Texto del Cap. 7 (comparación, campeón, robustez) + apéndices (ADF/KPSS, reproducibilidad).
4) Commit con resultados/figuras y actualizar PR #2.

— Referencia PR: https://github.com/joslat92/TesisProject/pull/2
